'use client'

import Icon from '@/assets/icons/View.svg';
import { IconProps } from './type';

export const View = ({
  className
}: IconProps) => {
  return <Icon className={className} />
}

export default View;