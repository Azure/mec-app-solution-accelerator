import Icon from '@/assets/icons/CameraDash.svg';
import { IconProps } from './type';

export const CameraDash = ({
  className
}: IconProps) => {
  return <Icon className={className} />
}

export default CameraDash;