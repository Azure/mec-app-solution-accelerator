'use client'

import Icon from '@/assets/icons/Plus.svg';
import { IconProps } from './type';

export const Plus = ({
  className
}: IconProps) => {
  return <Icon className={className} />
}

export default Plus;