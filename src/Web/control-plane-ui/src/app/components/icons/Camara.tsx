import Icon from '@/assets/icons/Camera.svg';
import { IconProps } from './type';

export const Camera = ({
  className
}: IconProps) => {
  return <Icon className={className} />
}

export default Camera;