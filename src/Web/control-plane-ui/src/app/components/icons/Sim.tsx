'use client'

import Icon from '@/assets/icons/SIM.svg';
import { IconProps } from './type';

export const Sim = ({
  className
}: IconProps) => {
  return <Icon className={className} />
}

export default Sim;