apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerts-ui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "alerts-ui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerts-ui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
